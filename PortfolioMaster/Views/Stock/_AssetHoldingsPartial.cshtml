@model IEnumerable<PortfolioMaster.Models.AssetHolding>

<table class="table">
    <!-- Table headers -->
    <thead>
        <!-- Add table headers for asset holding properties -->
    </thead>
    <tbody>
        @foreach (var holding in Model)
        {
                <tr>
                    <!-- Add table data for asset holding properties -->
                </tr>
        }
    </tbody>
</table>
